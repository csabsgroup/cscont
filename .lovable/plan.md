

# No Changes Needed

The condition field **"Parcelas vencidas"** (`installments_overdue`) already exists in `AutomationRulesTab.tsx` (line 109) with the following configuration:

- **Type**: number
- **Operators**: maior que, menor que, igual a

This field is already available when creating/editing automation rules. It will also be preserved and properly categorized (under "Contrato") when the expanded condition fields plan is implemented.

No code changes required.

